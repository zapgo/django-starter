# Configure Windows
Set-WindowsExplorerOptions -EnableShowHiddenFilesFoldersDrives -EnableShowFileExtensions

Update-ExecutionPolicy Unrestricted

## Basics
cinst -y PowerShell
cinst -y cmder -pre
cinst -y 7zip.install
cinst -y wget
cinst -y rsync
cinst -y vim
cinst -y curl

## Git
cinst -y git.install
cinst -y poshgit
cinst -y github

## Node, npm
cinst -y nodejs.install

npm install bower -g
npm install requirejs -g
npm install bower-requirejs -g

npm install less -g
npm install csso -g
npm install node-sass -g

npm install grunt -g
npm install grunt-cli -g
npm install grunt-contrib-less -g
npm install grunt-contrib-uglify -g
npm install grunt-sass -g
npm install grunt-bower-requirejs -g


## Editors
# cinst -y SublimeText3
# cinst -y SublimeText3.PackageControl
# cinst -y sublimetext3-contextmenu
# cinst -y SublimeText3.PowershellAlias
cinst -y notepadplusplus

if (Test-PendingReboot) { Invoke-Reboot }

## Python
$PackageName = 'miniconda'
$InstallerType = 'EXE'
$Url = 'https://repo.continuum.io/miniconda/Miniconda3-3.10.1-Windows-x86.exe'
$Url64 = 'https://repo.continuum.io/miniconda/Miniconda3-3.10.1-Windows-x86_64.exe'
$Args = "/InstallationType=AllUsers /RegisterPython=1 /AddToPath=1 /S"
$ValidExitCodes = @(0)
 
Install-ChocolateyPackage "$PackageName" "$InstallerType" "$Args" "$Url" "$Url64"  -validExitCodes $ValidExitCodes

pip install fabric3


## DevOps Stuff
# cinst -y virtualbox
# cinst -y virtualbox.extensionpack

# Docker is currently not included, since it doesn't support Windows 10 at this time.
# To install, manually download and run https://github.com/docker/toolbox/releases/download/v1.8.1a/DockerToolbox-1.8.1a-windows-10-test.exe

if (Test-PendingReboot) { Invoke-Reboot }

## Nice to have
cinst -y teamviewer
cinst -y firefox
cinst -y slack

# cinst -y sysinternals
# cinst -y DotNet3.5
# cinst -y DotNet4.0
# cinst -y DotNet4.5

if (Test-PendingReboot) { Invoke-Reboot }

# Pinning Things
# Install-ChocolateyPinnedTaskBarItem "$env:programfiles\Sublime Text 3\sublime_text.exe"
# Install-ChocolateyFileAssociation ".txt" "$env:programfiles\Sublime Text 3\sublime_text.exe"

# Let's get Updates, too
# Install-WindowsUpdate -acceptEula
